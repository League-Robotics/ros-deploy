---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart gz bridge
  ansible.builtin.systemd:
    name: "{{ gz_bridge_service_name }}"
    state: restarted
    daemon_reload: true
