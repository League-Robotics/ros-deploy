---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart swerve drive
  ansible.builtin.systemd:
    name: "{{ swerve_drive_service_name }}"
    state: restarted
    daemon_reload: true
