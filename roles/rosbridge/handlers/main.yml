---
# roles/rosbridge/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart rosbridge service
  ansible.builtin.systemd:
    name: "{{ rosbridge_service_name }}"
    state: restarted
