---
# roles/ros_docker/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart ros-docker
  ansible.builtin.systemd:
    name: ros-docker
    state: restarted
    daemon_reload: true
