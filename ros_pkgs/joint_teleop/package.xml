<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>joint_teleop</name>
  <version>0.1.0</version>
  <description>
    Joystick control of articulated joints by POSITION (std_msgs/Float64) —
    robot flippers, arms, grippers. Rate-based: a held control moves the target
    angle and it stays where released. Kept separate from the drive teleop
    packages because driving is chassis velocity while this is joint position;
    they fail differently and want different defaults.
  </description>
  <maintainer email="eric.busboom@jointheleague.org">Eric Busboom</maintainer>
  <license>MIT</license>

  <exec_depend>rclpy</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>std_msgs</exec_depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
