# inventory/host_vars/torture.yml
# Per-host overrides for the `torture` workstation (amd64, Ubuntu 22.04).
# Ubuntu 22.04 → ROS 2 Humble (Kilted requires 24.04).

ros_version: humble
ros_package_variant: ros-base
